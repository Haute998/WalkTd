@using WeModels
@{
    Layout = null;
}
@model SYSNotify
<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8">
    <title>修改</title>
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <script src="~/js/layui-v1.0.3/layui/lay/dest/layui.all.js"></script>
    <link href="~/js/layui-v1.0.3/layui/css/layui.css" rel="stylesheet" media="all" />
    <script src="~/js/jquery-1.10.2.min.js"></script>
</head>
<body>
    <fieldset class="layui-elem-field layui-field-title">
        <legend>系统消息</legend>
    </fieldset>
    <form class="layui-form" action="">
        @Html.HiddenFor(m => m.ID)
        <div class="layui-form-item">
            <label class="layui-form-label" style="width: 200px; ">消息类别</label>
            <div class="layui-input-inline" style="width:400px">
                @Html.TextBoxFor(m => m.MsgType, new { @class = "layui-input", @lay_verify = "required", @placeholder = "请输入消息类别", @autocomplete = "off", @required = "" })
            </div>
        </div>
        <div class="layui-form-item">
            <label class="layui-form-label" style="width: 200px; ">消息代号</label>
            <div class="layui-input-inline" style="width:400px">
                @Html.TextBoxFor(m => m.MsgCode, new { @class = "layui-input", @lay_verify = "required", @placeholder = "请输入消息代号", @autocomplete = "off", @required = "" })
            </div>
        </div>
        <div class="layui-form-item">
            <label class="layui-form-label" style="width: 200px; ">备注</label>
            <div class="layui-input-inline" style="width:400px">
                @Html.TextBoxFor(m => m.Remark, new { @class = "layui-input", @placeholder = "请输入备注", @autocomplete = "off" })
            </div>
        </div>

        <div class="layui-form-item">
            <label class="layui-form-label" style="width: 200px; "></label>
            <div class="layui-input-block">
                <button class="layui-btn" lay-submit lay-filter="go">保存修改</button>
            </div>
        </div>
    </form>

    <script>
        //Demo
        layui.use('form', function () {
            var form = layui.form();

            //监听提交
            form.on('submit(go)', function (data) {
                $.post("/SYSNotify/ToEdit", $("form").serialize(), function (rtn) {
                    if (rtn == "ok") {
                        parent.window.location.href = parent.window.location.href;
                    }
                    else {
                        layer.alert(rtn);
                    }
                });
                return false;
            });
        });
    </script>
</body>
</html>
